#!/usr/bin/env sh
set -e

if ! git diff --quiet; then
  echo "❌ Error: You have unstaged changes. Please 'git add' all changes before committing."
  echo ""
  echo "Unstaged files:"
  git diff --name-only
  exit 1
fi

echo "🔍 Running check..."
pnpm run check
echo "✅ All checks passed!"
